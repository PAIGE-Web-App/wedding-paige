# Enforce engine versions specified in package.json
engine-strict=true

# Use pnpm for this monorepo
# This will be enforced by the preinstall script in apps/web/package.json

# Hoist dependencies to root node_modules when possible
# This saves disk space and ensures consistent versions
shamefully-hoist=false

# Store pnpm store in a central location (optional)
# Uncomment if you want to customize the store location
# store-dir=~/.pnpm-store
